backend:
  name: github
  repo: peditx/starlight-theme-rapide  # ریپوی GitHub خودت
  branch: main
  # بجای OAuth از Personal Access Token استفاده می‌کنیم
  auth_type: token
  token: YOUR_GITHUB_PERSONAL_ACCESS_TOKEN  # ← اینو با PAT خودت جایگزین کن

media_folder: "docs/public/uploads"
public_folder: "/uploads"

collections:
  - name: "projects"
    label: "Projects"
    folder: "docs/src/content/docs"
    create: true
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Publish Date", name: "date", widget: "datetime", required: false }
      - { label: "Body", name: "body", widget: "markdown" }
